import {NextRequest,NextResponse} from "next/server";
export const runtime="nodejs";
export async function POST(req:NextRequest){const token=process.env.TELEGRAM_BOT_TOKEN;if(!token)return NextResponse.json({error:"TELEGRAM_BOT_TOKEN is not configured."},{status:503});const form=await req.formData();const audio=form.get("audio");const chatId=String(form.get("chatId")||"");const caption=String(form.get("caption")||"Rapsometeddy Voice Studio");if(!(audio instanceof File)||!chatId)return NextResponse.json({error:"audio and chatId are required."},{status:400});const tg=new FormData();tg.append("chat_id",chatId);tg.append("caption",caption);tg.append("document",audio,audio.name||"rapsometeddy-voice.wav");const res=await fetch(`https://api.telegram.org/bot${token}/sendDocument`,{method:"POST",body:tg});const data=await res.json();return NextResponse.json(data,{status:res.ok?200:502});}